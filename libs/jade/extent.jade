block settings
	- var title = "Extent switcher"
	- var sidebar = true
	- var toolbars = true

extends ../template/template.jade

block breadcrumbs
	+breadcrumbs([
		{title: "Home", href: "index.html"},
		{title: "Modules", href: "index.html#modules"},
		{title: "Modals", href: "index.html#modals"},
		{title: "Extent switcher"}])

block sidebar
	include ../template/sidebar-modules.jade

block content
	.dialog__mask(style="z-index: 2;")
	.dialog.dialog--extent-picker(aria-describedby="dialog-1-main", aria-labelledby="dialog-1-label", role="alertdialog", style="z-index: 3;", tabindex="-1")
		header.dialog__header
			h2.dialog__title#dialog-1-label Choose an country
			button.dialog__close(data-action="close", type="button")
				i.dialog__close-icon(aria-hidden="true")
				span.dialog__close-text Close

		div.dialog__main#dialog-1-main
			img(alt="Citizens Advice", onerror="this.onerror=null;this.src='/Static/images/logos/ca-logo_100px.svg.png';", src="/Static/images/logos/ca-logo_100px.svg")
			div
				h2 Where in the UK do you live?
				p The law can vary in different parts of the UK.

			.extent-selection-desktop
				div
					a.btn.btn-primary.extent-selection(href="?lang=en-GB") England
					a.btn.btn-primary.extent-selection(href="?lang=en-NIR") Northern Ireland
					a.btn.btn-primary.extent-selection(href="?lang=en-SCT") Scotland
					a.btn.btn-primary.extent-selection(href="?lang=en-WLS") Wales
					a.btn.btn-primary.extent-selection(href="?lang=cy") Cymru

		footer.dialog__footer

	//script.
		$(document).ready(function(){
			$("#showSimpleModal").click(function() {
				$("div#simpleModal").addClass("show");
				return false;
			});
			$("#closeSimple").click(function() {
				$("div#simpleModal").removeClass("show");
				return false;
			});
		});
		//close modal when clicking the esc keyboard button
		$(document).keyup(function(event){
			if(event.which=='27'){
				$("div#simpleModal").removeClass('show');
			}
		});